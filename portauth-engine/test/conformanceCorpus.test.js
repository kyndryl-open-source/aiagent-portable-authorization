// Copyright contributors to the aiagent-portable-authorization project.
// SPDX-License-Identifier: MIT
// This code is generated by AI, edited by humans

import assert from "node:assert/strict";
import { describe, it } from "node:test";
import { runConformance } from "../../conformance/run-conformance.js";

describe("Machine-readable conformance corpus", () => {
  it("passes all reference vectors", async () => {
    const outcome = await runConformance({ silent: true });
    assert.equal(outcome.pass, true, JSON.stringify(outcome.failures, null, 2));
  });
});
